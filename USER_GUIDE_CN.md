# VerdictQuant 1.0.0 使用教程

<p class="version">适用于 Windows 最终发行版。本文按实际界面和实际命令编写。</p>

> **先看安全边界：**这个版本只能做行情分析、研究记录和本地模拟交易。它不会连接银行、券商、钱包或真实订单接口。软件给出的方向、置信度和预测都不是收益保证。

如果你第一次接触 VerdictQuant，只需要先完成“10 分钟快速开始”。其余章节遇到问题时再查。

## 目录

1. [先理解三个功能入口](#three-entries)
2. [下载、解压和启动](#install)
3. [第一次配置 AI 模型](#model)
4. [完成一次 PA K 线分析](#analysis)
5. [怎样阅读分析结果](#result)
6. [增量分析、等待收盘和持续跟踪](#incremental)
7. [使用自动量化模拟账户](#paper)
8. [使用综合研究中心](#research)
9. [备份、恢复和重置模拟账户](#maintenance)
10. [让 Codex 或其他 AI 调用 CLI](#cli)
11. [常见问题排查](#troubleshooting)
12. [建议的日常使用流程](#daily)
13. [完整示例](#examples)
14. [必须保留的证据边界](#limits)

<a id="quick-start"></a>
## 10 分钟快速开始

1. 解压 `VerdictQuant-windows-x64.zip`，不要在压缩包里面直接运行。
2. 双击 `VerdictQuant.exe`。
3. 点击左上角 **AI 模型设置**，填写模型、Base URL 和模型提供商的 API Key，然后保存。
4. 打开 **PA K线分析**，选择 `TradingView`、交易所、股票代码和周期。
5. 点击 **获取数据**，看到 K 线后点击 **提交分析**。
6. 分析结束后先看右侧的 **决策** 和 **未来走势预期**。
7. 如果要验证这套判断能否形成可复核结果，进入 **综合研究中心 → 自动量化模拟**，再运行一次 **分析并进入模拟**。

第一次建议使用：

- 美股：`NASDAQ` + `SPCX` + `1d` + `100` 根 K 线。
- A 股：市场选 `A股` + `600519` + `1d` + `100` 根 K 线。
- 交易倾向保持 **均衡**，先不要使用“极度激进”。

---

<a id="three-entries"></a>
## 1. 先理解三个功能入口

| 入口 | 用来做什么 | 会不会写入模拟账本 |
| --- | --- | --- |
| **PA K线分析** | 获取 K 线，运行两阶段价格行为分析，阅读结构、决策和未来走势 | 不会 |
| **综合研究中心 → 自动量化模拟** | 对任意美股/A 股运行分析，并把合格信号交给本地风控和模拟订单引擎 | 可能会 |
| **综合研究中心的其他页面** | 阅读外部研究、回测、组合、基金和 Polymarket 模拟证据 | 不会写入股票模拟账本 |

最重要的区别：

- **提交分析**只是研究。
- **分析并进入模拟**才允许生成本地模拟意图。
- 即使进入模拟，风控也可以拒绝信号，或者等待下一根已收盘 K 线。
- 所有入口都禁止真实下单。

<a id="install"></a>
## 2. 下载、解压和启动

### 2.1 正确解压

把 ZIP 完整解压到一个普通目录，例如：

```text
D:\VerdictQuant\
```

目录中至少应当保留：

```text
VerdictQuant.exe
VerdictQuantCLI.exe
VerdictQuantUpdater.exe
_internal\
USER_GUIDE_CN.html
VERIFY_RELEASE.ps1
```

不要只复制两个 EXE；`_internal` 是程序运行所需依赖。

### 2.2 验证文件完整性

在程序目录打开 PowerShell，运行：

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File .\VERIFY_RELEASE.ps1
```

正常结果类似：

```text
Release verified: version=1.0.0 files=... paper_only=true
```

如果提示哈希不一致或文件缺失，重新解压原始 ZIP，不要继续运行残缺文件。

### 2.3 启动

双击 `VerdictQuant.exe`。第一次启动会创建当前 Windows 用户自己的本地模拟账户。

默认模拟资金：

- 美股账户：`100,000 USD`
- A 股账户：`1,000,000 CNY`

两个账户相互独立，也不代表你的真实持仓。

### 2.4 自动更新

带有 `VerdictQuantUpdater.exe` 的版本会在启动约 5 秒后后台检查 GitHub Release。默认行为是：

1. 发现更高版本号；
2. 自动下载 `VerdictQuant-windows-x64.zip`；
3. 同时校验 GitHub 返回的文件大小和 SHA-256 摘要；
4. 询问是否现在退出并安装；
5. 由安装目录之外的独立更新器替换程序，保留一个 `.backup` 回滚目录，然后自动重启。

也可以从 **帮助 → 检查更新** 手动检查，或从 **帮助 → 更新设置** 关闭自动检查/下载。

如果 GitHub 仓库仍是私有仓库，需要为每台电脑填写一个 fine-grained personal access token：

- 只选择 `Monkey-Sheep/VerdictQuant` 这一个仓库；
- Repository permissions 只授予 `Contents: Read-only`；
- 不要使用组织管理、写代码、工作流、Release 写入等权限；
- 仓库公开后清空该 Token，公开 Release 不需要登录。

更新 Token 与模型 API Key 一样，保存在当前 Windows 用户的凭据管理器和 DPAPI 密文中，不会明文写入 `settings.json`。首次从不含更新器的旧版本升级时，仍需手动下载并解压一次新版；从这个版本开始，后续升级不再需要手动拉 GitHub。

更新校验失败、ZIP 路径异常、程序未完全退出或替换失败时，更新器会停止并尽量恢复旧目录，不会继续运行未通过校验的新包。SHA-256 校验不能替代 Windows 代码签名；未购买代码签名证书前，Windows SmartScreen 仍可能显示“未知发布者”。

<a id="model"></a>
## 3. 第一次配置 AI 模型

点击顶部 **AI 模型设置**。

### 3.1 各字段怎么填

| 字段 | 填写内容 |
| --- | --- |
| **模型 (model)** | 你的模型提供商控制台实际支持的模型名 |
| **Base URL** | 提供商的 HTTPS API 根地址，例如 `https://api.deepseek.com` |
| **API Key** | 只填写模型提供商的 Key |
| **Thinking** | 开启会增加推理时间；遇到速度问题可以关闭 |
| **Reasoning Effort** | `low` 更快，`high`/`max` 更慢且消耗更多；首次建议 `medium` 或保留默认 |

不要填写券商密码、银行密码、助记词、交易验证码或钱包私钥。

API Key 会同时写入当前 Windows 用户的 **凭据管理器**，并保留一份仅该用户可解密的 **DPAPI 密文备份**。它不会明文写进项目的 `settings.json`；保存时程序还会立即读回校验，校验失败会提示错误而不会假装保存成功。正常重启后无需重新输入。

### 3.2 保存后怎样确认

保存设置后，顶部不应继续显示“未配置 API Key”。随后先做一次小规模分析。

如果出现 `401`、`model not found` 或连接失败：

1. 确认 Key 属于这个 Base URL 对应的提供商。
2. 确认模型名确实在提供商账户中可用。
3. Base URL 必须是完整 HTTPS 地址，不能包含账号密码、查询参数或反斜杠。
4. 不要把模型名和 Base URL 填反。
5. 如果提供商文档要求特定路径，以其官方文档为准。

<a id="analysis"></a>
## 4. 完成一次 PA K 线分析

打开顶部页签 **PA K线分析**。

### 4.1 选择数据来源

- **TradingView**：默认选项，不要求 TradingView 登录，适合美股、A 股、外汇、加密和部分期货行情。
- **MT5**：只有你主动选择时才会连接本机 MT5 终端；它仍然只读取行情，不会下单。

普通股票分析建议先用 TradingView。

### 4.2 填写交易所、代码和周期

常见示例：

| 资产 | 交易所 | 品种代码 | 周期 |
| --- | --- | --- | --- |
| SPCX | `NASDAQ` | `SPCX` | `1d` |
| Coinbase | `NASDAQ` | `COIN` | `1d` |
| ASML 美股 ADR | `NASDAQ` | `ASML` | `1d` |
| 贵州茅台 | `SSE` | `600519` | `1d` |
| 比特币行情研究 | 选择可用加密交易所 | `BTCUSDT` | `5m` 或 `1h` |

如果不知道交易所，可以先选 **（自动）**；自动探测可能更慢。美股已知在 NASDAQ/NYSE 时，手动选择更稳定。

支持的常用周期包括 `1m`、`5m`、`15m`、`1h`、`4h`、`1d`。

### 4.3 获取数据

点击 **获取数据**。看到 K 线图并且状态栏确认获取成功后，再提交分析。

最右侧浅色空心 K 线通常是未收盘 K 线，它可以实时显示，但不会参与 AI 已收盘 K 线分析。

### 4.4 提交分析

点击 **提交分析**。程序将运行：

```text
已收盘 K 线
  → 阶段一：市场诊断
  → 结构与一致性检查
  → 阶段二：交易决策
  → 决策、未来走势和证据记录
```

分析期间可以查看右侧 **实时** 页面。不要关闭程序，也不要频繁切换数据来源、代码或周期。

模型输出格式错误时，程序可能自动重试。重试是格式修复，不代表结果一定正确。

<a id="result"></a>
## 5. 怎样阅读分析结果

右侧结果区包含：

| 页面 | 主要内容 | 初学者是否必看 |
| --- | --- | --- |
| **实时** | 模型当前思考和生成进度 | 可选 |
| **决策树** | 每个判断节点及理由 | 建议复核 |
| **决策树可视化** | 图形化判断路径 | 可选 |
| **决策** | 方向、订单类型、入场、止损、止盈、置信度和禁止原因 | **必看** |
| **未来走势预期** | 情景、关键价位、失效条件和可选下根 K 线预期 | **必看** |
| **原始** | 原始结构化模型输出 | 排障使用 |
| **调试** | Prompt 和内部证据 | 开发排障使用 |

### 5.1 一条可执行研究结论至少应回答

1. 当前是偏多、偏空还是等待？
2. 依据是什么结构或价格行为？
3. 入场条件是什么，而不是只给一个方向？
4. 哪个价格或结构会使判断失效？
5. 止损和目标是否合理？
6. 如果没有触发条件，为什么选择“不下单”？

缺少失效条件、止损或清楚理由的结论，不应因为方向听起来合理就被采用。

### 5.2 四种交易倾向

在 **其他通用设置 → 交易倾向** 中可以选择：

| 倾向 | 作用 |
| --- | --- |
| **保守** | 更容易等待，要求更严格 |
| **均衡** | 默认，适合一般研究和模拟验证 |
| **激进** | 更愿意给出方案，但不代表更准确 |
| **极度激进** | 在未触犯硬性禁止时强制选择方向和进场方式 |

交易倾向只改变阶段二的决策偏好，不能提高数据质量，也不能消除风险。验证系统时建议保持 **均衡**，不要通过“极度激进”人为制造交易数量。

### 5.3 下根 K 线预期

在 **其他通用设置** 中开启“下根K线预期”后，结果会显示在 **未来走势预期**。

它是待验证预测，不是确定概率。系统会在后续 K 线收盘后记录命中情况和校准证据。

<a id="incremental"></a>
## 6. 增量分析、等待收盘和持续跟踪

### 6.1 等待最新 K 线收盘

勾选 **等待最新K线收盘后再提交分析** 后，点击提交会先等待当前 K 线走完，再用新收盘数据分析。

适合：

- 日线收盘后做复盘。
- 5 分钟或 15 分钟策略只使用完整 K 线。
- 避免未收盘 K 线反复改变形态。

### 6.2 增量分析

同一代码、同一周期存在上一轮成功记录，而且新增 K 线数量符合设置时，**提交分析** 按钮会自动变成 **增量分析**。

增量分析会延续上一轮上下文，重点检查新增 K 线是否确认、削弱或推翻原判断。它不是把上一轮结论原样复制。

### 6.3 持续跟踪分析

勾选 **持续跟踪分析** 后，每当出现新的已收盘 K 线，程序会自动开始下一轮分析。

注意：

- 每次启动程序时该选项都会恢复为未勾选，避免意外消耗 API Token。
- 只有程序保持运行时才会跟踪。
- 短周期会显著增加模型调用次数和费用。

<a id="paper"></a>
## 7. 使用自动量化模拟账户

进入 **综合研究中心 → 自动量化模拟**。

### 7.1 它怎样运行

```text
公开行情 + PA 两阶段分析
  → 标准化信号
  → 置信度和硬性规则
  → 风险仓位计算
  → 本地待成交订单
  → 下一根或更晚的已收盘 K 线模拟成交
  → 止损/止盈/时间退出
  → 复盘指标
```

分析所使用的当根 K 线永远不能成交自己的信号，这可以避免最明显的未来函数。

### 7.2 第一次运行

1. 选择 **美股** 或 **A股**。
2. 输入股票代码。
3. 美股填写 `NASDAQ` 或 `NYSE`；A 股交易所留空。
4. 第一次建议周期选 `1d`，K 线数量选 `100`。
5. 点击 **分析并进入模拟**。
6. 等待下方输出完成，然后查看账户、持仓和信号表。

出现 `不下单`、`rejected` 或没有新订单，不一定是故障。它可能表示：

- PA 没有形成清楚的入场、止损和失效条件。
- 置信度低于门槛。
- 同一股票已有持仓或待成交买单。
- 资金、单票上限或风险预算不足。
- 数据不完整、成交量为零或 K 线尚未收盘。

### 7.3 各按钮作用

| 按钮 | 作用 |
| --- | --- |
| **分析并进入模拟** | 分析当前股票；合格信号才进入本地模拟队列 |
| **结算后续K线** | 使用后续已收盘 K 线处理待成交、止损、止盈和退出 |
| **加入自动观察池** | 保存当前市场、代码、周期和 K 线数量 |
| **运行观察池周期** | 先结算已有订单，再逐个分析观察池股票 |
| **刷新模拟账户** | 重新读取本地账本，不发起分析 |
| **备份账户** | 备份账本、风控配置和观察池 |
| **恢复账户** | 校验备份并恢复；恢复前自动备份当前账户 |
| **重置账户** | 归档当前账本后创建新模拟账户 |

### 7.4 怎样理解账户摘要

- **权益**：现金加当前持仓按最新价格计算的价值。
- **现金**：尚未占用的模拟资金。
- **收益率**：相对于该模拟账户初始资金的结果。
- **已平仓**：已经形成完整进出场结果的交易数。
- **胜率**：盈利平仓数除以已平仓数；样本很小时没有统计意义。
- **证据样本未满30笔**：结果还未达到最低可复核数量。

达到 30 笔只代表“可以开始复核”，不代表策略可实盘，更不代表一定赚钱。系统中的实盘升级始终为禁用。

### 7.5 美股与 A 股规则差异

- 美股模拟以 1 股为最小单位。
- A 股买入以 100 股为一手。
- A 股执行 T+1，当天买入的模拟持仓不能当天卖出。
- 股票默认只做多；卖出主要用于退出已有多头持仓。

<a id="research"></a>
## 8. 使用综合研究中心

综合研究中心包含美股、A 股、Polymarket、证据与复盘、主动研究队列、组合与基金、集成引擎和自动量化模拟。

### 8.1 股票页面

美股或 A 股表格用于汇总外部研究证据。双击股票代码，可以把它载入 **PA K线分析** 页面继续分析。

### 8.2 Polymarket 页面

Polymarket 是独立的预测市场模拟证据，不属于股票账本，也不会影响美股或 A 股模拟资金。该版本不会连接钱包或提交真实 Polymarket 订单。

### 8.3 外部引擎不可用怎么办

部分研究页面依赖程序目录之外的 finance workspace 和可选上游引擎。把程序发给另一台电脑时，这些页面可能显示证据缺失或不可用。

这不影响核心功能：

- PA K 线分析
- 美股/A 股本地自动量化模拟
- 本地账户、备份和 CLI

不要为了让可选页面变绿而随意安装来源不明的软件或 Skill。

<a id="maintenance"></a>
## 9. 备份、恢复和重置模拟账户

### 9.1 备份

在自动量化模拟页面点击 **备份账户**。备份包括：

- 本地 SQLite 模拟账本
- 风控参数
- 自动观察池

备份不包括 API Key、Windows 凭据、银行信息或券商信息。

### 9.2 恢复

点击 **恢复账户**，选择备份 ZIP。程序会显示类似下面的确认语：

```text
RESTORE <profile_id>
```

必须原样输入。恢复前程序会自动备份当前账户，并检查文件白名单、大小、哈希、数据库结构和账户身份。

### 9.3 重置

点击 **重置账户** 后需要输入：

```text
RESET <profile_id>
```

重置会先归档旧账本，再创建新的本地模拟账户。不要把重置当成删除亏损记录的方法；验证策略时应保留失败样本。

### 9.4 本地数据位置

Windows 默认位置：

```text
%LOCALAPPDATA%\VerdictQuant\records\quant
```

同一个 Windows 用户升级或重新解压程序时，会继续使用同一个本地账户。高级环境变量覆盖会故意创建另一条数据边界，普通用户不要设置。

<a id="cli"></a>
## 10. 让 Codex 或其他 AI 调用 CLI

在程序目录打开 PowerShell。所有命令返回结构化 JSON，且不能连接真实账户。

### 10.1 先做健康检查

```powershell
.\VerdictQuantCLI.exe --pretty doctor
.\VerdictQuantCLI.exe --pretty status
```

重点确认：

```text
paper_only = true
live_execution = false
real_account_connection = false
database.schema_version = 3
live_promotion_allowed = false
```

### 10.2 只分析，不进入模拟订单

```powershell
.\VerdictQuantCLI.exe --pretty analyze `
  --market us --symbol SPCX --exchange NASDAQ `
  --timeframe 1d --bar-count 100 --predict-next-bar --no-queue
```

`--no-queue` 会记录研究证据，但不会创建或取消模拟订单。

### 10.3 分析并允许进入模拟

```powershell
.\VerdictQuantCLI.exe --pretty analyze `
  --market us --symbol SPCX --exchange NASDAQ `
  --timeframe 1d --bar-count 100 --predict-next-bar
```

### 10.4 A 股示例

```powershell
.\VerdictQuantCLI.exe --pretty analyze `
  --market a-share --symbol 600519 --timeframe 1d --bar-count 100
```

### 10.5 观察池和结算

```powershell
.\VerdictQuantCLI.exe --pretty watch-add `
  --market us --symbol ASML --exchange NASDAQ --timeframe 1d --bar-count 100

.\VerdictQuantCLI.exe --pretty cycle
.\VerdictQuantCLI.exe --pretty settle
.\VerdictQuantCLI.exe --pretty status
```

### 10.6 给 Codex 的安全调用说明

可以明确告诉 Codex：

```text
只允许调用当前目录的 VerdictQuantCLI.exe。
先运行 doctor 和 status，再使用 --no-queue 分析目标股票。
除非我明确要求模拟验证，否则不要创建模拟订单。
禁止连接银行、券商、钱包和任何真实订单接口。
请解释 thesis、催化剂、失效条件、仓位约束和证据状态。
```

这段说明不会让程序获得实盘能力，只是限制 AI 的调用顺序。

<a id="troubleshooting"></a>
## 11. 常见问题排查

### 11.1 双击没有反应

1. 确认已经完整解压，不是在 ZIP 内部运行。
2. 确认 `_internal` 与 EXE 在同一程序目录结构中。
3. 运行 `VERIFY_RELEASE.ps1`。
4. 在 PowerShell 运行 `VerdictQuantCLI.exe --pretty doctor` 查看具体错误。

### 11.2 填了真实 API Key 仍然不能分析

- Key 必须是模型提供商的 API Key，不是网页登录密码。
- 模型名必须是账户实际可用的模型。
- Base URL 与 Key 必须属于同一个提供商。
- 远程地址必须使用 HTTPS。
- `401` 通常是 Key/账户问题；`model not found` 通常是模型名问题；超时通常是网络或提供商问题。

### 11.3 TradingView 获取不到数据

- 检查交易所和代码是否匹配。
- 美股代码不要加交易所前缀，交易所在单独字段选择。
- A 股输入六位代码，例如 `600519`。
- 港股代码按界面提示去掉不必要的前导零。
- 自动探测慢时手动选择交易所。

### 11.4 分析很慢

- 关闭 Thinking 或把 Reasoning Effort 调成 `low`/`medium`。
- 减少用于分析的 K 线数量，例如从 500 调到 100。
- 使用日线时要等数据源响应；不要连续重复点击提交。
- 模型格式失败会触发重试，因此一次分析可能调用多次 API。

### 11.5 为什么分析完成却没有模拟买入

这是允许的正常结果。检查输出中的 `reason`、置信度、止损、资金上限、已有持仓和待成交订单。不要为了产生交易而盲目降低所有风控门槛。

### 11.6 为什么点击结算仍然没有成交

- 必须存在分析之后的已收盘 K 线。
- 限价单或突破单必须满足价格条件。
- 不能使用分析当根 K 线成交。
- A 股还受到 100 股一手和 T+1 约束。

### 11.7 综合研究中心某些页面没有数据

这些页面可能依赖外部 finance workspace。先确认 **PA K线分析** 和 **自动量化模拟** 是否正常；核心发行版不要求所有外部引擎都存在。

### 11.8 模拟账户怎么突然变了

普通安装始终使用当前 Windows 用户的固定账户。检查是否设置了 `VERDICTQUANT_DATA_HOME`、`VERDICTQUANT_QUANT_HOME`、`VERDICTQUANT_QUANT_DB` 或 `VERDICTQUANT_QUANT_CONFIG`；这些高级覆盖会创建独立数据边界。旧版 `PA_AGENT_*` 变量仅用于升级兼容。

<a id="daily"></a>
## 12. 建议的日常使用流程

### 开盘前或收盘后

1. 查看新闻、公司公告和宏观事件。
2. 在 PA K线分析中检查重点股票日线。
3. 阅读决策、未来走势、失效条件，不只看“涨/跌”。
4. 只分析时使用 GUI 的 **提交分析** 或 CLI 的 `--no-queue`。

### 模拟验证

1. 对 thesis 清楚的标的运行 **分析并进入模拟**。
2. 把长期跟踪标的加入观察池。
3. 新 K 线收盘后运行观察池周期或结算。
4. 查看持仓、平仓原因、净收益、回撤和校准。

### 每周复盘

1. 查看已平仓样本数，而不是只看账户总收益。
2. 区分止损、止盈、时间退出和显式卖出。
3. 检查盈利是否集中在单一股票或单一行情阶段。
4. 保留亏损和无交易样本，避免只看成功案例。
5. 修改策略前先备份账户，并记录修改日期。

<a id="examples"></a>
## 13. 完整示例

### 13.1 SPCX 日线研究

1. 进入 **PA K线分析**。
2. 数据来源选 `TradingView`。
3. 交易所选 `NASDAQ`。
4. 品种填 `SPCX`，周期选 `1d`。
5. 点击 **获取数据**。
6. 数据成功后点击 **提交分析**。
7. 在 **决策** 中记录方向、入场条件、止损、目标和不下单原因。
8. 在 **未来走势预期** 中记录关键支撑、阻力和失效条件。
9. 只有想验证规则时，才到 **自动量化模拟** 再运行一次。

### 13.2 A 股日线模拟

1. 进入 **综合研究中心 → 自动量化模拟**。
2. 市场选 `A股`。
3. 股票代码填 `600519`，交易所保持空白。
4. 周期选 `1d`，K 线数量选 `100`。
5. 点击 **分析并进入模拟**。
6. 如果出现待成交信号，等下一个交易日出现新收盘 K 线后点击 **结算后续K线**。
7. 如果当天模拟买入，T+1 规则会阻止同日卖出。

示例代码仅用于说明软件操作，不代表建议买入该股票。

<a id="limits"></a>
## 14. 必须保留的证据边界

这个最终版解决的是工程可靠性和模拟可复核性，不是“已经证明赚钱”。

- OHLC K 线不能还原真实盘口队列、部分成交和滑点深度。
- A 股涨跌停封单、公司行动、退市、复权和幸存者偏差需要额外验证。
- 模型置信度不是天然校准概率；必须用后续结果计算 Brier 等校准指标。
- 30 笔平仓只是最低复核门槛，不是实盘授权。
- 回测、模拟和历史准确率都不能保证未来收益。
- 软件始终保持 `paper_only=true`、`live_execution=false`。

正确目标是：让每个判断有输入、模型、策略、信号、模拟成交和结果证据，能够复盘哪里有效、哪里失败，再决定是否继续改进。

---

## 一句话记忆

```text
先获取已收盘数据 → 再做两阶段分析 → 看失效条件 → 进入模拟验证
→ 等后续 K 线结算 → 累积足够样本 → 复盘，不把预测当保证。
```
